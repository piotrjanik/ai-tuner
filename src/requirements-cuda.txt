# Dependencies for CUDA training (Colab, Vertex AI, RunPod, Lambda, etc.)
# Note: torch, datasets, tqdm, pyyaml, huggingface_hub are typically
# pre-installed on Colab/Vertex. Listing only what's missing or needs upgrading.
#
# IMPORTANT: transformers and trl versions must match what Unsloth expects.
# Check Unsloth's official notebooks for the latest pinned versions:
# https://github.com/unslothai/notebooks
unsloth                   # 2x faster LoRA fine-tuning on NVIDIA GPUs
transformers==4.56.2      # pinned — Unsloth requires exact version
trl==0.22.2               # pinned — install with --no-deps to avoid conflicts
peft>=0.13.0
bitsandbytes>=0.44.0
accelerate>=1.0.0
xformers
typer>=0.12.0