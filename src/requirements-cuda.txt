# Dependencies for CUDA training (Colab, Vertex AI, RunPod, Lambda, etc.)
# Note: torch, transformers, datasets, tqdm, pyyaml, huggingface_hub are
# pre-installed on Colab/Vertex. Listing only what's missing or needs upgrading.
unsloth>=2024.11.0        # 2x faster LoRA fine-tuning on NVIDIA GPUs
peft>=0.13.0
trl>=0.12.0
bitsandbytes>=0.44.0
accelerate>=1.0.0
typer>=0.12.0